{
  "name": "tsonic-nodejs-workspace",
  "private": true,
  "description": "Repo for @tsonic/nodejs portable source surface",
  "type": "module",
  "scripts": {
    "generate:10": "echo '@tsonic/nodejs is source-authored; no generation step'",
    "selftest": "node --check versions/10/runtime/index.js",
    "publish:10": "npm run selftest && npm publish ./versions/10 --access public"
  },
  "devDependencies": {
    "@types/node": "^20.14.0"
  }
}
